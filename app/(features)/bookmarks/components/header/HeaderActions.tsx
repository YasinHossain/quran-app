'use client';

import React from 'react';

export const HeaderActions = (): React.JSX.Element => (
  <div className="flex items-center gap-2 flex-shrink-0">
    {/* Header actions removed - no new folder functionality */}
  </div>
);
